import React from 'react';

import Layout from '../../layouts/Layout'
const HomePage
 = (props) => {
    return (
            <Layout>
                
            </Layout>
    );
};

export default HomePage
;