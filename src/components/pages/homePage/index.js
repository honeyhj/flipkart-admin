import React from 'react';
import Layout from '../../layouts/Layout'
const HomePage
 = (props) => {
    return (
            <Layout>
                    homePage
            </Layout>
    );
};

export default HomePage
;