import React from 'react';
import Layout from './layouts/Layout';

const Onno = () => {
    return (
        <Layout sidebar>
            <h2>all categories</h2>
        </Layout>
    );
};

export default Onno;